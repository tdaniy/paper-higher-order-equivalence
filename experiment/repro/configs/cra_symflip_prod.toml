[meta]
alpha = 0.05
base_seed = 20260227

[cra_symflip]
# Asymptotic-focused grid (9 points, log-ish)
n_grid = [2000, 3000, 4500, 6800, 10000, 15000, 22000, 33000, 50000]

# Fewer f-curves
f_grid = [0.50]
near_census_c = [1]

# Monte Carlo (balanced for cost vs signal)
B = 200
R = 8000

# DGP (skewed baseline, constant effect)
tau = 0.5
mu = 0.0
s = 1.0
g_dist = "lognormal"
g_log_sigma = 0.5
g_gamma_shape = 4.0

# Pairing / one-sided
use_crn = true
one_sided = "upper"
