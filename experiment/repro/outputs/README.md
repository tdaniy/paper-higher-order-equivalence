# Outputs

Generated numeric outputs (CSV/NPZ). Use one subfolder per run:

```
outputs/<experiment>/run_<config>_seed<seed>_hash<short>/tables/
```

Each experiment directory should include a `summary.csv` indexing all runs.
